{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["TURBO_*"],
  "tasks": {
    "//#lint:prettier": {},
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$"]
    },
    "lint": {
      "dependsOn": ["//#lint:prettier", "lint:eslint", "lint:svelte", "lint:tsc"]
    },
    "lint:eslint": {
      "dependsOn": ["^build", "^lint:eslint"]
    },
    "lint:svelte": {
      "dependsOn": ["^build", "^lint:svelte"]
    },
    "lint:tsc": {
      "dependsOn": ["^build", "^lint:tsc"]
    }
  },
  "ui": "tui"
}
