# NodeJS
.pnpm-store
node_modules

# IDEs
.idea

# Temp files & build artifacts
.DS_Store
.turbo
.prettiercache
build/
temp/

# Local config
.claude/settings.local.json
# Agent-framework session artifacts (backups, state) — never commit to the public fork
.claude/backups/
.claude/teams/
.claude/tasks/
